# Salin file ini menjadi .env.local untuk development lokal
# Untuk production (Vercel/Netlify), set env variable ini di dashboard-nya

# URL Flask ML API yang sudah di-deploy ke Render
VITE_ML_API_URL=https://bdrain-ml-api.onrender.com
